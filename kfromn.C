#include <stdio.h>
int main()
{
    int n, k, i;
    printf("Enter the two nos ");
    scanf("%d %d", &n, &k);
printf("enter the array of nos ");
scanf("%d",&n);
for(k=1;k<i;++i)
printf("%d ",++i);
return(0);
}
